version: "3.4"

services:
    server:
        container_name: pivo-server
        build:
            context: ./
            args:
                - NODE_ENV=production
        ports: 
            - "3000:3000"
        networks:
            - network

networks:
    network:
        driver: bridge
